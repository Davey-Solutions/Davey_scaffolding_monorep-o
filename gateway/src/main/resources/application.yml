spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: job-service
          uri: http://job-service:8080
          predicates:
            - Path=/api/v1/jobs/**

        - id: auth-service
          uri: http://auth-service:8080
          predicates:
            - Path=/api/v1/auth/**

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
